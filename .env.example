# Data Sovereignty stack configuration.
#
# `make env` copies this to .env (never overwrites) and generates the Airflow
# secrets. Fill in the values marked REQUIRED before `make up`.
#
# .env is git-ignored. Never commit real values, never echo them in logs.
#
# It is created once and never re-synced, so anything set here shadows the
# docker-compose.yml default permanently: pinning an image tag in .env turns a
# later bump in git into a silent no-op on a long-lived host. Secrets and
# host-specific values belong here; non-secret tunables belong in
# docker-compose.yml and are listed below commented out, for discoverability only.

# ─── Required secrets ────────────────────────────────────────────────────────

# Metabase Enterprise production license token. Nothing in this repo needs one —
# without it the instance boots and behaves like OSS, and ingestion, quality and
# the warehouse are unaffected. It only gates Enterprise features, none of which
# this stack itself uses.
MB_PREMIUM_EMBEDDING_TOKEN=

# Metabase setup-wizard admin. The password must satisfy MB_PASSWORD_COMPLEXITY.
MB_ADMIN_EMAIL=ignacio@metabase.com
MB_ADMIN_PASSWORD=

# Written by scripts/bootstrap_metabase.sh — leave blank on a fresh checkout.
MB_API_KEY=

# ─── Source credentials ──────────────────────────────────────────────────────
# One per connected source, and there are none on a fresh checkout — this stack
# ingests nothing until a source is connected.
#
# A source spec (`sources/<name>.yml`) names the variable holding its token, in
# `api.auth.token_env`; the token itself is never in the spec, which is what lets
# specs live in a public repo. Add the variable here under that exact name and
# compose passes it through — connecting a credential needs no code or compose
# change. The add-source skill writes the spec and tells you which name to add.
#
# For example, a spec with `token_env: PYLON_API_KEY` wants:
#   PYLON_API_KEY=

# ─── Warehouse ───────────────────────────────────────────────────────────────

WAREHOUSE_DB=warehouse
WAREHOUSE_USER=warehouse
WAREHOUSE_PASSWORD=warehouse

# dlt destination credentials. These are the HOST-facing values (running the
# CLI from your laptop). Containers get warehouse-db:5432 injected by compose,
# so you never have to think about which side you are on.
DESTINATION__CLICKHOUSE__CREDENTIALS__HOST=localhost
DESTINATION__CLICKHOUSE__CREDENTIALS__PORT=9001
DESTINATION__CLICKHOUSE__CREDENTIALS__HTTP_PORT=8124
DESTINATION__CLICKHOUSE__CREDENTIALS__SECURE=0
DESTINATION__CLICKHOUSE__CREDENTIALS__USERNAME=warehouse
DESTINATION__CLICKHOUSE__CREDENTIALS__PASSWORD=warehouse
# Only the database a connection selects; every table name in this stack is
# qualified. Each source lands in its own raw_<source>, set per run by the ingest
# runtime, so this must NOT name one.
DESTINATION__CLICKHOUSE__CREDENTIALS__DATABASE=default

# ─── Metabase application database ───────────────────────────────────────────

METABASE_APP_DB=metabaseapp
METABASE_APP_USER=metabase
METABASE_APP_PASSWORD=metabase

# ─── Airflow ─────────────────────────────────────────────────────────────────

AIRFLOW_DB=airflow
AIRFLOW_USER=airflow
AIRFLOW_PASSWORD=airflow

# Generated by `make env` (scripts/gen_secrets.sh). Rotating them invalidates
# existing Airflow connections/variables encrypted with the old Fernet key.
AIRFLOW__CORE__FERNET_KEY=
AIRFLOW__API_AUTH__JWT_SECRET=

# Host UID that owns the bind-mounted airflow/logs directory. On a Linux host it
# must be the owning user's real uid (`id -u`): the containers write into the bind
# mounts as this uid, and a mismatch leaves those files unwritable by the host user
# — which a deploy that runs `git reset --hard` then fails on. Docker Desktop maps
# uids through its VM, so the mismatch is invisible on macOS.
AIRFLOW_UID=50000

# ─── Tunables ────────────────────────────────────────────────────────────────
# Already defaulted where they are consumed; the effective value is shown. Set
# one here only to override it on this host.

# METABASE_IMAGE=metabase/metabase-enterprise:v1.63.1.6
# CLICKHOUSE_IMAGE=clickhouse/clickhouse-server:24.8-alpine
# CLICKHOUSE_MEM_LIMIT=3g
# Raise these alongside CLICKHOUSE_MEM_LIMIT, same headroom ratio below it —
# ClickHouse's own internal ceiling, read via low-memory.xml's `from_env`.
# Left unset, ClickHouse gets a laptop-sized budget regardless of how big the
# container is, which undersells a production host; raised without also
# raising CLICKHOUSE_MEM_LIMIT to match, the internal ceiling can exceed the
# container's cgroup limit, which OOM-kills the whole container instead of
# ClickHouse cleanly erroring one query.
# CLICKHOUSE_SERVER_MEMORY_USAGE=2000000000
# CLICKHOUSE_MARK_CACHE_SIZE=268435456
# CLICKHOUSE_UNCOMPRESSED_CACHE_SIZE=134217728
# METABASE_MEM_LIMIT=2g
# Raise with METABASE_MEM_LIMIT, never on its own — the JVM must stay inside it.
# METABASE_JAVA_OPTS=-Xmx1500m

# ─── Host ports ──────────────────────────────────────────────────────────────
# Chosen to not collide with the metabase-demo stack (3000/5433).

METABASE_HOST_PORT=3100
AIRFLOW_HOST_PORT=8080
WAREHOUSE_HTTP_PORT=8124
WAREHOUSE_NATIVE_PORT=9001
DATADOCS_HOST_PORT=8081

# ─── Metabase bootstrap ──────────────────────────────────────────────────────

MB_URL=http://localhost:3100
# Quote any value containing a space: scripts source this file with `set -a`,
# and bash would otherwise read the second word as a command.
MB_SITE_NAME="Data Sovereignty"
# Headless credential storage — no OS keychain prompts in containers or CI.
MB_CLI_DISABLE_KEYRING=1
# Display name of the warehouse connection in Metabase. bootstrap_metabase.sh
# matches on the physical connection, so renaming it after the fact is safe.
# MB_WAREHOUSE_DB_NAME=Warehouse

# ─── Data quality ────────────────────────────────────────────────────────────

# Freshness SLO override, in hours, applied to EVERY source at once. Each source
# declares its own under `quality.freshness.hours`, which is where it belongs —
# how often a source changes is a fact about that source. Set this only to loosen
# them all for one run without editing the contracts.
# GX_FRESHNESS_HOURS=24

# ─── Escape hatches (leave commented unless you know you need them) ──────────
# Only for a STAGING Metabase license token. With a production token the
# instance runs in normal mode and these must stay unset.
# MB_RUN_MODE=e2e
# METASTORE_DEV_SERVER_URL=https://token-check.staging.metabase.com
